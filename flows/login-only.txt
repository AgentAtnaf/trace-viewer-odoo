# Smoke test — just verify login works and take a screenshot.
# Replace <PLACEHOLDERS> before running.
#
# Usage:
#   ./trace.sh SMOKE http://<ODOO_HOST>:8069/web/login < flows/login-only.txt

# `login` auto-skips if the saved session (.sessions/) is still valid
login <ADMIN_USER> <ADMIN_PASS>
url
screenshot
done
