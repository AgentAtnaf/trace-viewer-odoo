# navmenu — QUICK mode (Odoo 14, dv_kc)
# Same destination, but jumps straight to #action=706 (deterministic, no menu
# clicks). The @706 after the path is the action id used by quick mode; the
# labels are kept for readability and as the fallback if quick can't resolve.
#
# Run:
#   ./trace.sh navmenu-quick 'http://localhost:8069/web/login?db=dv_kc' < flows/navmenu-quick.txt

login admin Odoo2024.!
mode quick
navmenu Inventory > Configuration > Products > Purchase Product Group @706
waitidle
screenshot navmenu-quick-final
url
done
